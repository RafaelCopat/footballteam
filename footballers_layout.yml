Footballers:
  extends: footballers # from hand.yml
  font: ChunkFive Roman,Sans 16
  color: '#F3EFE3'
  align: center
  spacing: -15

Art:
  extends: art # from hand.yml

Cost:
  extends: cost # from hand.yml
  font: Sans 18
  color: '#F3EFE3'
  
CostIcon:
  extends: cost # from hand.yml
  x: -= 75
  y: -= 50

Description:
  extends: description # from hand.yml
  font: Sans 10
  align: center
  valign: middle
  color: '#F3EFE3'

Defence:
  extends: bonus1 # from hand.yml
  font: ChunkFive Roman,Sans 12
  color: '#F3EFE3'
  #y: += 70
DefenceIcon:
  extends: bonus1 # from hand.yml
  x: -= 100
  width: -= 40
  height: -= 40

Assisting:
  extends: Defence
  y: += 100
AssistingIcon:
  extends: DefenceIcon
  y: += 100

Shooting:
  extends: Assisting
  y: += 100
ShootingIcon:
  extends: AssistingIcon
  y: += 100